import React from "react";

export default function Home() {
  const style = {
    gridColumn: "1 / span 2",
    alignSelf: "center",
    justifySelf: "center",
    fontSize: "4rem",
  };

  return <div style={style}>HOME</div>;
}
